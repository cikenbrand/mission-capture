# Test fixtures

Seeded into the portable config directory by `Copy-Fixture`. Subdirectories
mirror the config layout:

- `jobs/` — scene collections (a Job)
- `rigs/` — profiles (a Rig), including `features.ini` variants
- `data/` — simulator input files (Phase 3)
- `golden/` — golden UI manifests and screenshots

Empty for now: suite T0 needs no fixtures, and each later phase adds its own.
